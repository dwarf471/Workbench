# 需求提取：步骤 1 人工审核

## 本次交付

大模型设置页、独立配置接口、Windows 当前用户 DPAPI 加密保存 API Key、固定文本连接测试。本步不包含聊天提取、候选需求或 CSV 登记。

服务地址：<http://127.0.0.1:8765/>，刷新后选择“大模型设置”。原有归档与数据库结构保持不变。

## 验收操作

1. 填写 OpenAI 兼容的 API 基础地址（通常以 `/v1` 结尾）及模型名称，按需填写 API Key。
2. 保存，确认 Key 输入被清空，标签显示已配置；刷新后配置保留，但不展示 Key。
3. 点击测试连接。操作先保存配置，再发送固定文本 `Connection test. Reply OK.`，输出限制为 32 tokens；可能产生少量模型调用费用。它不查询数据库、不调用 MCP、不发送聊天记录。
4. 留空 Key 再保存，应保留原有凭证；勾选清除并保存，应显示未配置。
5. 远程和本机均支持 HTTP/HTTPS；填写 HTTP 地址时页面显示明文传输警告。确认错误提示不显示密钥、原始服务响应或底层异常。

接口使用 `{base_url}/chat/completions`，要求服务返回非空 `choices[0].message.content`。不兼容的接口协议或模型参数会提示连接失败，不能将连通性测试视为后续需求分析质量验证。

## 保存与安全边界

配置位于 Git 忽略的 `data/model.local.json`；地址、模型和超时明文保存，Key 加密保存，GET 接口只返回是否已配置。与 EpointMsg 设置互不影响。

凭证绑定当前 Windows 用户/环境，复制文件到其他账号或机器后不保证可解密；需要重新填写 Key。Python 字符串不能保证内存中的明文副本完全清除。用户自行选择信任的模型端点；测试会将 Key 发送给该端点。

远程和本机端点均允许 HTTP/HTTPS。HTTP 会明文传输 API Key 和请求内容，仅应在可信网络使用，优先选择 HTTPS。禁用自动重定向及环境代理继承，不回显服务原始响应。请求超时可设置 5–300 秒，默认 120 秒。尚未使用真实模型地址和凭证进行外部连接验收。

## 自动验证

后端测试覆盖 DPAPI 往返、凭证保留与清除、非法地址不回显输入、固定测试请求、认证失败、重定向阻断、非法响应、超时和损坏凭证。测试使用临时配置与模拟模型服务，不使用生产凭证。

浏览器在桌面 1440px 与窄屏 390px 验证保存、测试和清除凭证，模型请求完全模拟；截图位于 `data/screenshots/model-1440.png` 和 `model-390.png`。前端 TypeScript 校验与构建通过，保留原有整包体积警告。

审核通过并明确指示后，再实施步骤 2：按单会话查询时间范围提取、后台任务和候选结果预览。
