# 阶段 3 人工审核

## 已交付

首页聊天归档，包含来源选择、会话名称搜索、私聊/群聊筛选、聊天消息分页、中文消息/文件名搜索、发送者和时间筛选、全局搜索与上下文定位、JSON/CSV 筛选导出、完整数据库在线备份及备份命令。

服务地址：http://127.0.0.1:8765 。刷新现有页面即可使用。此阶段无数据库结构变更，迁移版本仍为 0002。原有归档和任务保留。

## 人工验收建议

1. 选择自己的来源，确认左侧已归档会话、消息数量和同步状态。
2. 搜索会话名称，筛选私聊或群聊，点击会话查看消息；超过 50 条时翻页，核对发送者和客户端时间。
3. 切换全局搜索，查询一个已知中文关键词；按发送者与时间范围过滤，点击查询。
4. 点击搜索结果的查看上下文，确认切回对应会话、定位正确页并高亮消息。
5. 从导出菜单分别下载 JSON、CSV，核对选定来源及筛选条件；导出包含所有匹配消息，不限当前页。
6. 临时关闭新点客户端，确认已有会话、消息、搜索和导出仍正常。这里只读本地 SQLite，不会隐式连接 MCP。
7. 点击备份数据库并确认，获得完整数据库文件。备份包括全部来源，不仅当前会话；也可使用根目录 backup.cmd。

姓名缺失时显示发送者 ID。关键词是字面中文子串匹配，英文区分大小写，不做分词或语义搜索。媒体展示类型、名称及元信息，不下载或在线加载媒体。

## 验证结果

- 后端 44 项测试通过，覆盖旧阶段功能，以及归档范围隔离、中文和特殊字符搜索、筛选范围、等时间戳分页、消息定位、凭证清洗、JSON/CSV 导出及公式防护、WAL 在线备份、备份下载、并发备份拒绝和安全错误提示。
- Playwright 桌面 1440px、窄屏 390px 共 6 项测试通过，覆盖连接检查、模拟同步、模拟离线聊天展示、HTML 安全转义、搜索定位、筛选导出和备份下载；无页面脚本错误和横向溢出。
- TypeScript 校验及正式构建通过；仍有原依赖弃用与整包体积警告，不影响运行。
- 本机真实在线备份已验证：包含 171 条已有消息，integrity_check 为 ok；原数据库消息数保持 171。
- 截图位于 data/screenshots/archive-1440.png、archive-390.png，以及 archive-focus-1440.png、archive-focus-390.png，仅含模拟数据。

浏览器下载测试曾出现原生下载绕过页面级模拟拦截，已改为上下文级拦截和独立模拟来源，并修正下载触发方式；生成的含临时导出内容的失败报告已清理。原始归档未修改，聊天未对外发送。

自动离线验证使用独立数据库，不关闭用户正在使用的新点客户端；真实客户端关闭后的人工验收请按上述步骤执行。

## 备份与隐私

备份使用 SQLite backup 接口，而非复制正在运行的主数据库文件。生成后核对 integrity_check，通过唯一名称保存于 data/backups，可通过受限文件名接口下载。备份和导出包含私人聊天，均不加密；自行保管并决定是否交给 AI 分析。本版没有外部 AI 调用、自动删除数据或在线恢复功能。

下一阶段或新功能须人工审核后另外授权。
