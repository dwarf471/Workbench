# 阶段 2 人工审核

## 已交付

来源会话发现、联系人/群组查找、跨页选择、私聊/群聊筛选、最近 30 天/自定义/全部可获取历史范围、手动同步、分页入库去重、持久化任务进度、取消、失败重试及重启中断后继续。服务地址：http://127.0.0.1:8765 。刷新已有页面即可使用同步管理。

数据库迁移版本 0002。阶段 3 的聊天详情浏览、全局内容搜索和导出没有提前实现。查找联系人用于发现来源会话，不是归档聊天全文搜索。

## 建议人工验收

1. 在连接设置确认来源显示名称及来源标识；标识是本地隔离键，不代表从新点认证出的当前账号。
2. 在同步管理点击发现会话，或输入联系人/群组名查找。选择一个私聊和一个群聊，可跨页保留选择。
3. 首次建议选一个小的自定义时间范围，确认当前新点账号与来源标识一致，点击开始同步。
4. 查看各会话页数、拉取数、新增数、实际范围及状态；范围拉取结束只表示当前 MCP 分页结束，不等于全量云端备份。
5. 对相同会话和范围再创建任务，已有相同消息不应重复新增；拉取计数包含边界重叠，因此可能高于新增数。
6. 对较大范围尝试取消；已有提交页面保留。关闭新点产生连接失败后，重新打开客户端并对失败任务继续/重试。
7. 后端停止或重启时未完成任务显示已中断；确认客户端账号后手动继续。来源或地址变化时旧任务不能继续，应切回任务原配置。

每个任务最多 100 个会话。后端运行期间串行执行任务，关闭网页不会取消任务；停止后端不会自动恢复任务。新点客户端本身切换账号无法自动识别，须人工保持来源一致。

## 验证范围

真实 MCP 握手及会话发现通过，当前返回 351 个最近会话；服务只向前端投影名称、类型和 ID，没有消息预览。

后端测试使用临时数据库和模拟 MCP，覆盖归档事务、分页重叠去重、凭证清洗、断点恢复、游标停滞警告、取消及请求中取消、账户隔离、账号变化阻断、401 重读、API 校验及队列操作。浏览器桌面和窄屏测试覆盖连接设置和模拟手动同步。

当前结果：后端 30 项测试通过，Playwright 4 项测试通过，前端 TypeScript 校验及正式构建通过。保留的依赖弃用与整包体积警告不影响运行。

未擅自选择真实会话拉取历史记录。测试截图中的任务 #999 和测试群是模拟响应，不是生产归档数据。真实私聊/群聊历史归档需要按上面的人工操作验收。

## 分页限制

按页事务提交消息和时间游标，确保失败重试不跳过未提交页。毫秒时间边界保留重叠；如果返回的最早时间不再前进或有更多数据却返回空页，则标记需关注，防止无限循环。只有时间游标而没有消息级游标时，无法保证恢复被客户端截断的同毫秒消息；警告重试不会绕过这个限制。

未知消息唯一 ID 的回退去重可能合并完全相同的无 ID 消息；不会据此声称全量备份，也不推断消息撤回或删除状态。
