# 需求提取：步骤 3 人工审核

## 本次交付

人工确认后的 CSV 需求登记、统一清单查看和下载、完全重复防护、疑似相似提醒、文件锁、原子替换、失败恢复及外部修改保护。

步骤 1、步骤 2、步骤 3 均已人工审核通过。不扩展需求生命周期管理或自动排期。

服务：<http://127.0.0.1:8765/>，刷新后使用。迁移版本 `0004`，新增 `requirement_records`、`requirement_export`，提取任务增加登记回执字段。升级前已在线备份，升级后原有 196 条消息及 1 个提取任务保留，完整性检查为 `ok`。自动测试未登记生产需求。

## 人工验收顺序

1. 打开已完成的需求提取任务，核对原文和概述，修改待确认事项，勾选需要备案的候选。
2. 点击登记需求，自动保存本次编辑，弹出确认窗口，显示选中项、预计新增/跳过数量及疑似相似需求。返回修改不会备案。
3. 完全重复项自动跳过；疑似相似项仍可登记，确认不需要时返回取消勾选。勾选人工确认后点击确认登记。
4. 确认登记回执、新增/跳过数量和对应需求编号。登记后本任务候选全部锁定，未勾选项不登记；无修改、删除或撤销登记入口，需要补充可重新提取并审核新任务。
5. 在聊天归档顶部点击需求清单，查看全部来源的已登记需求、概述、待确认事项、状态及来源编号，下载 CSV。
6. CSV 保存到 `data/requirements/需求清单.csv`，UTF-8 BOM，一项需求一条物理数据行。验证逗号、引号正确转义，文本换行折叠，公式前缀保护。
7. 对同一证据再次提取并登记相同概述，应跳过完全重复项。不同概述或不同证据不强行合并，疑似相似提醒仅供人工参考。
8. 若文件被 Excel 等程序占用，登记可能已入库但 CSV 写入失败；关闭占用后在需求清单点击重新生成 CSV，不会再次新增需求。

## 文件与数据库一致性

SQLite 是已确认需求的权威登记簿，CSV 是其统一备案输出，包含全部来源，不按当前会话筛选。字段：需求编号、登记时间、来源账号/名称、会话、查询起止时间、概述、待确认事项、来源消息编号、状态、任务及候选编号。CSV 时间显式使用 `+08:00`，截止日毫秒边界保留。

登记事务一次提交新增记录及任务回执，再从全部登记记录生成 CSV。进程间文件锁串行化操作，临时文件写入并刷新后原子替换；失败不会用半成品覆盖现有文件。文件写入失败不会撤销已完成的数据库备案，页面提示重新生成。重复提交同一任务返回原回执，不重复登记。

服务重启时尝试恢复已有登记记录的 CSV；也可以显式重新生成。如果文件内容与上次输出及当前登记簿均不匹配，判为外部修改并保留，拒绝覆盖。先另存或移走该文件，再重新生成；工作台不导入手工 CSV 修改，修改版应单独保管。下载仅允许完整、最新且无冲突的清单。

完全重复键由来源、会话、排序后的证据编号和归一化概述组成，不含查询范围和备注。重复登记不会覆盖已有备注。相似提醒比较最近 500 条同来源同会话需求，通过证据重叠或文本相似度启发式检测，每项最多显示 5 条；不是语义去重保证。

确认窗口使用候选更新时间防止确认后内容变化，登记时重新校验。预计数量可能受并发登记影响，以最终回执为准。默认状态待评审，人工备案不代表需求已经开发或验收。

## 验证与隐私

- 后端 97 项测试通过，使用临时数据库/目录；覆盖选中项、回执及锁定、任务内/跨任务重复、相似提醒、人工确认及过期预览、CSV BOM/转义/公式/单行保护、并发提交及文件锁。
- 模拟数据库提交失败、文件替换失败、替换后元数据提交失败；验证回滚、已备案未导出提示、旧文件保留、重试和恢复。
- 外部 CSV 修改不会被登记、重新生成或启动恢复覆盖。
- 桌面 1440px、窄屏 390px 浏览器回归覆盖归档、同步、模型配置、提取和登记下载；模型与提取/登记请求完全模拟，不访问生产聊天模型接口。
- TypeScript 校验和构建通过，保留依赖弃用及整包体积警告。新增依赖 `filelock==3.19.1`，启动脚本自动安装。

截图位于 `data/screenshots/registration-confirm-1440.png`、`registration-confirm-390.png`、`requirement-ledger-1440.png` 和 `requirement-ledger-390.png`，仅含模拟数据。

清单、数据库及备份含私人资料且未加密，均在 Git 忽略范围内。完整数据库备份包含登记簿和回执，不包含独立 CSV；恢复数据库后可重新生成，若旧 CSV 不匹配先另存或移走。登记/查看/下载/恢复只操作本地数据，不调用模型或 MCP。

自动实施验证期间未向真实模型发送聊天，未创建生产 CSV。随后人工验收已通过；本文件保留验收步骤及验证记录。
