# 阶段 1 人工审核

## 当前结果

已完成 Vue 3 / TypeScript / Element Plus 前端、FastAPI 后端、SQLite / SQLAlchemy 引擎、Alembic 初始迁移、连接配置及官方 Python MCP SDK 适配器。

数据库为空的 sources 来源登记表和 alembic_version，迁移版本 0001。没有聊天表、会话拉取、历史消息调用或同步任务。下一步需人工审核通过后明确授权。

## 审核入口

服务地址：http://127.0.0.1:8765 。当前已在后台运行，后续可用项目根目录 start.cmd 启动；端口被占用时启动脚本会停止并提示，不会结束其他进程。

1. 确认页面显示 SQLite 已就绪、迁移版本 0001。
2. 确认客户端配置文件路径和 MCP 服务地址正确。
3. 点击检查连接，确认连接正常，并显示 rongyun-im-mcp、协议及四个工具。
4. 修改账户显示名称并保存，刷新确认保留。此名称是本地标签，不是从新点认证出的账号身份。
5. 如需检查失败提示，可暂时将地址改成一个未监听的本机端口，检查后恢复原地址并保存。

## 自动验证

- 前端 TypeScript 校验和正式构建通过。
- 后端 14 项测试通过：加密及历史明文令牌、非法封装、地址校验、数据库迁移、配置读写、跨站和 Host 防护、错误凭证读取的安全提示。
- Playwright 桌面 1440px、窄屏 390px 两项测试通过：数据库展示、真实 MCP 检查、工具展示、无脚本报错及无横向溢出。
- 截图保存在 data/screenshots/connection-1440.png 和 connection-390.png。
- 真实连接检查只调用 initialize 和 tools/list，未读取聊天内容。

已修复 Windows 注册表将 JavaScript MIME 类型映射为 text/plain 所导致的空白页面问题，后端显式返回 application/javascript。

## 已知说明

- 工作台配置只保存账户标签、MCP 地址和新点配置路径。令牌只在内存中处理，不保存到工作台文件或数据库。
- 仅本机访问，没有远程认证，不应改成公网或局域网监听。
- 当前 SDK 测试客户端有两条依赖弃用警告，不影响通过；前端整包构建存在体积警告，可在页面模块增多时按需拆分。
- 浏览器测试会保存当前默认配置，不修改新点自身的配置。
- 原始令牌解密留存文档保持不变。
