# 需求提取：步骤 2 人工审核

## 本次交付

聊天归档的提取需求按钮、发送前确认、持久化消息快照、串行后台分析、分段与合并、取消和中断状态、候选概述/待确认事项编辑、选择与草稿保存、来源消息追溯。

仅交付候选预览，不写入 CSV 或正式需求清单文件。步骤 1 已审核通过，本步骤需另外审核。

服务地址：<http://127.0.0.1:8765/>，刷新后使用。数据库迁移升级到 `0003`，新增 `extraction_tasks`，既有归档表不变。升级前已在线备份；升级后 196 条原有消息保持不变，完整性检查为 `ok`。

## 人工验收

1. 在大模型设置确认接口、模型和凭证可用。
2. 在会话浏览选择一个私聊或群聊，选择开始、截止日期，点击查询。范围为浏览器本地时区开始日 00:00:00.000 至截止日 23:59:59.999，覆盖截止日整天。必须成功查询且有完整日期范围；全局搜索不支持提取。
3. 点击提取需求，核对冻结的会话、范围、消息数、未分析数量和模型地址。返回不会发出模型请求。
4. 勾选发送确认后开始分析。未重新查询的输入修改不改变本次范围；关键词/发送者过滤不沿用，分析范围内全部发出和收到的可用文本，不限当前页。
5. 查看分段进度，关闭预览或网页不停止后台任务。重新打开当前会话，可查看最近 20 个非草稿任务。
6. 核对概述、待确认事项和展开的来源消息。修改或取消勾选后保存候选草稿；关闭并重新查看，修改应保留。浏览器不能改写证据编号。
7. 尝试取消任务。已发出的文本无法撤回，取消不保存部分候选。后端停止/重启时等待或运行任务标记中断，不自动发送；需重新准备并确认新任务。

无需求时显示空清单。请求失败、响应无效、证据编号非法、响应截断或任一分段失败，不提供部分成功候选。

## 分析边界与容量

- 只读取本地库，不调用 MCP 或补拉历史。起止时间均包含。
- 按时间和本地编号冻结文字、发送者名称或匿名参与者编号、客户端时间、发出/收到方向。不发送完整消息 JSON、媒体链接、本地媒体路径或原始发送者 ID。
- 无文本消息计为未分析，图片/语音/附件实体不访问。其他类型已有的文字可作为文字材料，不代表理解其媒体内容。
- 每次最多 10000 条消息、300000 个序列化字符；每段目标容量 12000 个序列化字符，单条超过其一半时拒绝，不截断。
- 相邻段保留最多两条上下文；多段候选及其证据参与合并，合并输入超过 60000 个序列化字符时失败，需缩小范围。
- 每轮输出最多 4096 tokens、200 项候选，每项最多 100 条证据；结构化 JSON 与证据编号必须校验通过。聊天中的指令不作为执行指令，没有模型工具调用。
- 同时最多一个等待或运行的提取任务。重复开始同一任务不重复创建；失败和中断不自动重试。
- 准备后模型配置变化会阻止开始；运行任务使用启动时的配置。API Key 不保存到任务快照。

模型仍可能漏提、误提或概括不准，证据编号合法不保证业务正确，需人工审核。清洗不保证识别正文所有秘密。外部模型接收选定文字，HTTP 接口明文传输且调用可能产生费用。

## 自动验证

- 后端 84 项测试通过：临时数据库及模拟模型，覆盖范围、来源、快照、确认、配置变化、重复开始、分段合并/失败、结构与证据校验、候选保存、取消请求及重启中断。
- 桌面 1440px、窄屏 390px 共 8 项浏览器测试通过，覆盖同步、归档、模型配置、发送确认、候选编辑和证据查看；模型与提取请求完全模拟。
- TypeScript 校验及构建通过，保留原依赖弃用与整包体积警告。未重跑会修改生产配置的真实 MCP 连接浏览器测试。
- 截图位于 `data/screenshots/extraction-confirm-1440.png`、`extraction-confirm-390.png`、`extraction-preview-1440.png` 和 `extraction-preview-390.png`，仅含模拟记录。

快照与候选包含私人资料，保存在 Git 忽略的 SQLite 中，也会进入完整备份。本次未上传 GitHub、未生成正式需求文件、未向真实模型发送生产聊天。

## 下一步

审核通过并明确指示后，实施步骤 3：人工确认后的 CSV 登记、防重复、写入恢复和完整验收。
